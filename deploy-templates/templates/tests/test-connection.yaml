apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python38gsonar.fullname" . }}-test-connection"
  labels:
    {{- include "python38gsonar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python38gsonar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
