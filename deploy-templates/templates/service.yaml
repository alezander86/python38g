apiVersion: v1
kind: Service
metadata:
  name: {{ include "python38gsonar.fullname" . }}
  labels:
    {{- include "python38gsonar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python38gsonar.selectorLabels" . | nindent 4 }}
