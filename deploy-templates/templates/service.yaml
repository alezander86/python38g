apiVersion: v1
kind: Service
metadata:
  name: {{ include "python38g2.fullname" . }}
  labels:
    {{- include "python38g2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python38g2.selectorLabels" . | nindent 4 }}
